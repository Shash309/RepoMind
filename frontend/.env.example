# ─── Frontend (Vercel) ───────────────────────────────────────────────────────
# Set this to your Render backend URL in the Vercel dashboard.
# In local development, leave empty — next.config.mjs falls back to localhost:3001.
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
